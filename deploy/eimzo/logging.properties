# e-imzo-server v2.1.1 logging configuration -- the vendor's own file, copied
# verbatim from the distribution (fix round 1, finding 4). The vendor's
# Dockerfile copies this into config/ alongside config.properties and points
# `-Djava.util.logging.config.file` at it; ours does the same.

handlers = java.util.logging.ConsoleHandler
.level = INFO
java.util.logging.ConsoleHandler.level = INFO
#java.util.logging.ConsoleHandler.formatter = java.util.logging.SimpleFormatter
#java.util.logging.SimpleFormatter.format= [%1$tF %1$tT] [%4$-7s] %5$s %n

uz.yt.cams.pki.level = WARNING
