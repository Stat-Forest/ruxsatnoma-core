# AGENTS.md

All agent instructions for this repository live in **[CLAUDE.md](CLAUDE.md)** —
the single source of truth. Read it in full before doing anything: it covers the
run/test commands, the module boundaries, and the hard rules that this codebase
is reviewed against.

Then read **[.claude/lessons.md](.claude/lessons.md)** — the project's accumulated
gotchas. If a lesson covers the area you are about to touch, it overrides your
instinct.

Product context, the master plan and the accepted decisions live in the sibling
docs repository: `../docs/status.md` (entry point), `../docs/plan.md`,
`../docs/decisions.md` (source of truth).

Do not duplicate content into this file — update `CLAUDE.md` instead.
