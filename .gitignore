.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
minio-data/
pg-data/
.DS_Store
.idea/
.vscode/
.superpowers/

# .claude holds tooling scratch (worktrees, local settings) next to files we DO
# track — ignore the directory and re-admit only what belongs in the repo.
.claude/*
!.claude/lessons.md
!.claude/commands/
!.claude/skills/

# pytest-testmon's per-worktree map of which test runs which code (make check)
.testmondata
.testmondata-*
